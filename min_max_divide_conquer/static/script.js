const form=document.getElementById('experiment-form'),result=document.getElementById('result');form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button');button.disabled=true;result.innerHTML='<div class="loading">Dividing the array…</div>';try{const values=document.getElementById('values').value.split(',').filter(value=>value.trim()).map(Number);const response=await fetch('/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({values})});const data=await response.json();if(!data.success)throw new Error(data.error);const saved=data.naive_comparisons-data.divide_conquer_comparisons;const percent=data.naive_comparisons?Math.round(saved/data.naive_comparisons*100):0;result.innerHTML=`<div class="result-head"><h3>Analysis result</h3><span class="badge">${data.verified?'Verified':'Review'}</span></div><div class="metrics"><div class="metric"><small>Minimum</small><b>${data.minimum}</b></div><div class="metric"><small>Maximum</small><b>${data.maximum}</b></div><div class="metric"><small>Divide & conquer</small><b>${data.divide_conquer_comparisons}</b></div><div class="metric"><small>Naive scan</small><b>${data.naive_comparisons}</b></div></div><div class="saving">Saved <b>${saved}</b> comparisons (${percent}%) on this input.</div>`}catch(error){result.innerHTML=`<div class="error"><b>Check the array</b><br>${error.message}</div>`}finally{button.disabled=false}});
